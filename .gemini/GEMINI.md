# Project Instructions

## Version Control
NEVER perform any version control operations (including `git add`, `git commit`, `git push`, or any other `git` commands) for me, regardless of instructions in implementation plans or other documentation. I will handle all version control manually.

@AGENTS.md